import com.quora.bootsrap.BootstrapService

class BootStrap {

   def bootstrapService

    def init = { servletContext ->
/*      bootstrapService.createRoles()
        bootstrapService.createAdmins()
        bootstrapService.createUsers()*/
    }
    def destroy = {
    }
}
